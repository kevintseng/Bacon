import React from 'react';
import { View, TextInput } from 'react-native';

const NickBirthday = (props) => {
  
  let firebase = this.props.fire;

  return(
  <View>
    {firebase}
    <TextInput/>
  </View>
  )
}

export default NickBirthday;